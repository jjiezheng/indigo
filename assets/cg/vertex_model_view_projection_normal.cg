struct Output {
	float4 position : POSITION;
};

Output main(
	float3 position : POSITION,
	uniform float4x4 model,
	uniform float4x4 view,
	uniform float4x4 projection) {

	Output OUT;
	OUT.position = projection * view * model * vertex;

  return OUT;
}