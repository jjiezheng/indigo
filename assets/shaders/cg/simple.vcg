struct VS_OUTPUT  {
	float4 vPosition : POSITION;
};

VS_OUTPUT main(float4 position : POSITION,
			   float4 normal   : NORMAL,
			   uniform float4x4 WorldViewProj) {
	VS_OUTPUT vout;
	vout.vPosition = mul(WorldViewProj, position);
	return vout;
}